import { Component } from '@angular/core';

@Component({
    standalone: true,
    selector: 'app-footer',
    template: ` 
    <div class="bg-primary-100">
    <span>Coffeecode</span>
    </div>

    `
})
export class AppFooter {}
